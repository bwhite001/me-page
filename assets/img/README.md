# Avatar image

Drop your avatar here as `avatar.jpg` (recommended: 300×300px square, JPG or PNG).

When ready, uncomment the `picture:` line in `_config.yml`:

```yaml
author:
  picture: /assets/img/avatar.jpg
```

Hydejack will show a letter-avatar automatically until this file is added.
